#pragma once

#include <string>

class Token_constants {
    public:
        static constexpr unsigned char NAME = 'T';
        static constexpr unsigned char NUMERAL = 'N';

        static constexpr char const * const DEFINE_VAR = "let";
};